# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LaarcIOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LaarcIOS
  pod 'Firebase/Core'
  pod 'Firebase/Database'

  target 'LaarcIOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LaarcIOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'LaarcIOSShareExtension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LaarcIOSShareExtension

end
